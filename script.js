const langBtn=document.getElementById('langBtn');const menuBtn=document.getElementById('menuBtn');const navLinks=document.getElementById('navLinks');let lang='en';menuBtn.addEventListener('click',()=>navLinks.classList.toggle('show'));langBtn.addEventListener('click',()=>{lang=lang==='en'?'hi':'en';document.querySelectorAll('[data-en]').forEach(el=>{el.textContent=el.getAttribute(`data-${lang}`)});langBtn.textContent=lang==='en'?'हिन्दी':'English';});
